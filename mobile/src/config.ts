/**
 * ThreatIQ Mobile App Configuration
 */

// Production Gateway URL
export const API_BASE_URL = "https://threatiq-gateway-service.onrender.com";

// App Theme Colors (matching web shadcn theme)
export const Colors = {
    // Base colors
    background: "#ffffff",
    foreground: "#0a0a0a",
    card: "#ffffff",
    cardForeground: "#0a0a0a",

    // Primary (blue)
    primary: "#2563eb",
    primaryForeground: "#ffffff",

    // Secondary (gray)
    secondary: "#f4f4f5",
    secondaryForeground: "#18181b",

    // Muted
    muted: "#f4f4f5",
    mutedForeground: "#71717a",

    // Accent
    accent: "#f4f4f5",
    accentForeground: "#18181b",

    // Semantic colors
    destructive: "#ef4444",
    destructiveForeground: "#ffffff",
    success: "#22c55e",
    warning: "#f59e0b",

    // Feature colors
    orange: "#f97316",
    purple: "#a855f7",
    blue: "#3b82f6",
    green: "#22c55e",

    // Border
    border: "#e4e4e7",
    input: "#e4e4e7",
    ring: "#2563eb",
};

// Spacing
export const Spacing = {
    xs: 4,
    sm: 8,
    md: 12,
    lg: 16,
    xl: 20,
    xxl: 24,
};

// Border Radius
export const BorderRadius = {
    sm: 6,
    md: 12,
    lg: 16,
    xl: 24,
    full: 9999,
};

// Font sizes
export const FontSizes = {
    xs: 12,
    sm: 14,
    base: 16,
    lg: 18,
    xl: 20,
    "2xl": 24,
    "3xl": 30,
};
